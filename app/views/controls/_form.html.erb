<%= form_for(@control) do |f| %>
  <% if @control.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@control.errors.count, "error") %> prohibited this control from being saved:</h2>

      <ul>
      <% @control.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :orderSizeLimit %><br />
    <%= f.number_field :orderSizeLimit %>
  </div>
  <div class="field">
    <%= f.label :longPositionLimit %><br />
    <%= f.number_field :longPositionLimit %>
  </div>
  <div class="field">
    <%= f.label :shortPositionLimit %><br />
    <%= f.number_field :shortPositionLimit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

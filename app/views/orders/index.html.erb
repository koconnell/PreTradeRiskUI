<h3 class="content-header">Orders:</h3>

<table>
  <tr>
    <th> AccountID </th>
    <th> InstrumentID </th>
    <th> Side </th>
    <th> Quantity </th>
    <th> Price </th>
    <th> Venue </th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @orders.each do |order| %>
  <tr>
    <td><%= order.accountID %></td>
    <td><%= order.instrumentID %></td>
    <td><%= order.side %></td>
    <td><%= order.quantity %></td>
    <td><%= order.price %></td>
    <td><%= order.venue %></td>
    <td><%= link_to 'Show', order %></td>
    <td><%= link_to 'Edit', edit_order_path(order) %></td>
    <td><%= link_to 'Destroy', order, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Order', new_order_path %>
<br />
<br />
<%= link_to 'Pre-Trade Risk UI Home Page', root_path %>
